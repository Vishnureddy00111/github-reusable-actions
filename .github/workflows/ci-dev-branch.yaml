name: Cart CI Pipeline
on:
  push:
    branches:
      - '*'
      - '!master'
run-name: ${{ github.sha }} | ${{ github.event.head_commit.message }}
jobs:
  tests:
    runs-on: self-hosted
    needs: build
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo npm test

